extends layout 

block content
    br
    br
    br
    .container.centerContainer
        h5(class = "authentication") You have been logout, you will have to login again to use this functionality !
    span(class="restricted")
        h2.pageTitle Please choose the method of adding your phenotypes definitions
        .container.centerContainer
            h5 There are actually two ways to add phenotype definitions on Phenoflow.
            a(href = "/phenoflow/definePage/API").btn.btn-outline-success.defineBtn Add through API 
            a(href = "/phenoflow/phenotype/define").btn.btn-outline-success.defineBtn() Add graphically
